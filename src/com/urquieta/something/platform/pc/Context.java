package com.urquieta.something.platform.pc;

public class Context {
    // NOTE(Misael): This class exist because android needs the
    // activity context, wich the PC build does not need. So this is gone be empty!
}
